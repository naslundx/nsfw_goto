name: Publish to PyPI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    name: 'Ubuntu, python 3.12'
    steps:
      - name: Publish to PyPI
        run: |
          poetry publish --build